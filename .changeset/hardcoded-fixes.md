---
"@hulistmi/hulistmi": patch
---

Fix hardcoded values that caused incorrect behavior: stale User-Agent version, broken /bot page link, duplicated Huawei URL builders, protocol-relative search URL mangling, and duplicated max query length.